import { addNode } from './add.tsx';
import { subNode } from './sub.tsx';
import { divNode } from './div.tsx';
import { mulNode } from './mul.tsx';

export const customNodes = [
  addNode,
  subNode,
  divNode,
  mulNode
];